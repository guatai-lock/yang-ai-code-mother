spring:
  application:
    name: yang-ai-code-mother-backend
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/yang_ai_code_mother
      username: root
      password: 160450
  profiles:
    active: local
server:
  port: 8123
  servlet:
    context-path: /api
  # mysql
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.guatai.yangaicodemother.controller
knife4j:
  enable: true
  setting:
    language: zh_cn
